# SQL 兼容性契约 — 单一事实源
#
# 背景（issue #23 / #29）：方法 C（手动 docker compose pull）和 Watchtower 用户只换 Grafana
# 镜像、不会重跑 SQL 安装脚本。#23 装了新镜像但 tou_rates 表/set_default_charging_rate 函数
# 没装上；#29 缺 lat_for_map 导致地图类仪表盘全部失效。这两次事故的共同根因是「镜像版本」和
# 「数据库里实际装的 SQL」没有任何机制互相校验，用户只能等报错才发现。
#
# SQL_COMPAT_REVISION 就是这道校验的唯一事实源：
#   - 只覆盖「兼容性三组 SQL」：sql/install-coord-functions.sql（坐标转换）、
#     sql/install-unit-functions.sql（单位换算）、sql/install-tou.sql（分时电价）。
#   - 不覆盖 sql/install-indexes.sql（性能索引）——三条安装路径一致把索引失败当 WARN
#     （"不影响功能，仅查询略慢"），纳入 revision 会导致「只有索引失败也让整个 bundle
#     显示过期」的矛盾。索引状态是独立信号，不掺进这个数字。
#
# 谁读这个文件：
#   - Dockerfile：COPY 进镜像 /opt/teslamate-cn/versions.env（一字不差的文件拷贝，不是
#     手动抄一遍数字），scripts/diagnose.sh 从运行中的 grafana 容器读这份镜像内文件，
#     取得"镜像要求的 revision"——这一步专为方法 C / Watchtower 用户设计，他们不会跑
#     任何脚本，diagnose 必须能只凭一个跑着的容器就读到 required revision。
#   - simple-deploy.sh / migrate-from-official.sh：curl-piped 单文件脚本，无法 source
#     本地文件，现场从同一个 REF 拉取这个文件（跟拉 SQL 文件同一套信任模型），避免任何
#     地方手动硬编码数字造成漂移。
#   - scripts/upgrade.sh：本地仓库检出，git pull 之后直接 source 这个文件。
#
# 何时 bump：三组 SQL 里任意一个新增/修改了对象签名（新函数、改参数、加表、加列且旧仪表盘
# 依赖新列）时 +1。纯注释、纯格式化不用 bump。
#
# CI 门禁（scripts/check-sql-revision.sh，接在 .github/workflows/ghcr-build.yml 的 lint job
# 里）会解析三组 SQL 的「对象契约」（表列/函数签名/视图名/触发器/索引，忽略注释和函数体）
# 并与 scripts/sql-contract-baseline.txt 比对：契约变了但这个数字没跟着 +1 会直接挡住合并。
# legitimate 的改动流程：改 SQL → bump 这个数字 → 本地跑
# `bash scripts/check-sql-revision.sh --update-baseline` 刷新基线 → 一起提交。
SQL_COMPAT_REVISION=3
